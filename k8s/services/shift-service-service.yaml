apiVersion: v1
kind: Service
metadata:
  name: shift-service
  labels:
    app: shift-service
spec:
  type: ClusterIP
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP
    name: http
  selector:
    app: shift-service
  sessionAffinity: None

